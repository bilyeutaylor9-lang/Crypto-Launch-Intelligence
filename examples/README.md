# Example Reports

These files are generated by `npm run demo` from built-in research seed projects.

They let GitHub visitors inspect the AI Council, Quantum Brain, Research OS, Alpha Lab, CSV export, and HTML dashboard without connecting API keys.

Open `report.html` locally, or inspect the JSON files directly.
